# 配息紀錄月更新 SOP

> **觸發方式**：每月 1 號（或任何時候），對 Claude 說「**更新上月配息**」
> Claude 無法自動排程，需由 Will 啟動對話

---

## 一、Claude 執行步驟

### ① 確認待查清單
從持股中篩出「該月有除息或發放」的標的：

| 代號 | 名稱 | 配息頻率 | 除息月 | 發放月（約除息後3週）|
|---|---|---|---|---|
| 0050 | 元大台灣50 | 半年配 | 1、7 | 2、8 |
| 0056 | 元大高股息 | 季配 | 1、4、7、10 | 2、5、8、11 |
| 006208 | 富邦台50 | 半年配 | 7、11 | 8、12 |
| 2330 | 台積電 | 季配 | 3、6、9、12 | 4、7、10、1 |
| 00927 | 群益半導體收益 | 季配 | 1、4、7、10 | 2、5、8、11 |
| 00894 | 中信小資高價30 | 季配 | 2、5、8、11 | 3、6、9、12 |
| 00881 | 國泰台灣科技龍頭 | 半年配 | 1、7 | 2、8 |
| 00981A | 主動統一台股增長 | 季配 | 1、4、7、10 | 2、5、8、11 |
| 00935 | 野村臺灣新科技50 | 半年配 | 3、9 | 4、10 |
| 00631L | 元大台灣50正2 | 年配 | 11 | 12 |
| 2812 | 台中銀 | 年配 | 7 | 8 |

### ② 搜尋實際配息金額
每檔逐一查證，**不可用推估值**：

```
搜尋關鍵字範例：
  "0056 2026年X月 配息 每股 除息日"
  "006208 富邦台50 配息金額 除息 發放日"
  "2330 台積電 季配息 每股 除息 入帳"
```

**優先來源**：
1. 投信官網公告（元大、富邦、群益、國泰、統一、野村投信）
2. TWSE 配息行事曆
3. 財經媒體（鉅亨、經濟日報、商周 ETF 指南）

**必須取得三項**：每股配息金額、除息日、股利發放日

### ③ 寫入配息紀錄
更新 `/mnt/user-data/outputs/配息紀錄_範本.csv`，欄位：

```
入帳日期,代號,名稱,券商,股數,每股配息,實收金額,狀態,備註
```

- **入帳日期**：用「股利發放日」，非除息日
- **券商**：同一檔跨帳戶要分列（如 006208 分永豐竹科 20000 股、台銀 1000 股）
- **股數**：從 Google Drive 工作表1 取當時持股
- **實收金額**：股數 × 每股配息（元）
- **狀態**：`已入帳`（發放日已過）／`已公告`（已宣告未發放）

### ④ 更新儀表板配息月曆分頁
重算每月合計與狀態顏色，未公告月份留空。

### ⑤ 驗證後推送
- CSV 逐列 `股數 × 每股配息 = 實收金額` 交叉驗算
- 月合計與明細表加總一致
- JS 語法 + CSS 完整性檢查
- 全通過才 push

---

## 二、驗證程式碼

```python
import csv
rows = list(csv.DictReader(open('配息紀錄_範本.csv', encoding='utf-8')))
errs = []
for i, r in enumerate(rows, 2):
    calc = int(r['股數']) * float(r['每股配息'])
    if abs(calc - float(r['實收金額'])) > 1:
        errs.append(f"第{i}列 {r['代號']}：{r['股數']}×{r['每股配息']}={calc:.0f}≠{r['實收金額']}")
    if r['狀態'] not in ('已入帳', '已公告'):
        errs.append(f"第{i}列 狀態值異常：{r['狀態']}")
assert not errs, errs
```

---

## 三、Will 的操作

**每月 1 號說一句**：

```
更新上月配息
```

Claude 會回報：
- 該月查到哪幾檔、每股各配多少
- 實收金額合計
- 哪些查不到（需人工補）
- 儀表板更新結果

**建議同時做**：對照券商對帳單核對金額。網路公告是「每股配息」，實際入帳可能因**二代健保補充保費**（單筆 2 萬以上扣 2.11%）或**手續費**而略少。

---

## 四、限制說明

| 項目 | 說明 |
|---|---|
| 自動排程 | ❌ Claude 無背景執行能力，需 Will 開對話觸發 |
| 資料來源 | 網路公開公告，非券商實際入帳紀錄 |
| 補充保費 | 公告金額未扣，實收會略低 |
| 建議做法 | 以券商對帳單為準，網路資料僅供快速填表 |

**提高可靠度的替代方案**：直接在 Google Sheets 的「配息紀錄」工作表手動記錄券商實際入帳金額，Claude 更新儀表板時讀取即可，完全免除網路查證誤差。

---

*RetireFlow 配息更新 SOP ｜ 2026/08/01 建立*
